/**
 * Bật/tắt tạm các tính năng trên giao diện (chỉ ẩn khỏi menu/tìm kiếm,
 * route và dữ liệu vẫn giữ nguyên).
 */
export const FEATURES = {
  /** Họp giao ban — tạm ẩn vì chưa dùng. */
  meetings: false,
} as const;
